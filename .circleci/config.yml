version: 2
jobs:
  build:
    docker:
    - image: selenium/standalone-chrome:3.1.0
    steps:
    - checkout
    - run: xvfb-run java -jar selenium-server-standalone.jar >/dev/null 2>&1 &
    - run: java -jar Test.jar 
workflows:
  version: 2
  workflow:
    jobs:
    - build
